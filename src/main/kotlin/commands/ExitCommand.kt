package commands

/**
 * Класс, представляющий собой команду, которая
 * завершает работу программы(без сохранения в файл)
 */
class ExitCommand : Command {


    constructor()
            : super("exit", "завершает работу программы(без сохранения в файл)")

}